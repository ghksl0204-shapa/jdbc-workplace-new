<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="animalMapper">
  
  	<insert id="save"
  	    	parameterType="animal">
		INSERT
		  INTO
		       ANIMAL
		VALUES
		       (
		       ANIMAL_ID = SEQ_ANIMAL_ID.NEXTVAL,
		       #{animalId},
		       #{animalName},
		       #{speciesId},
		       #{zoneId},
		       #{keeperId},
		       #{gender},
		       #{weightKg},
		       SYSDATE,
		       SYSDATE
		       )         	
  	</insert>
  
  </mapper>